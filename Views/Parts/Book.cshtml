@{
    var arthor = (string)Model.Arthor;
    var language = (string)Model.Language;
    var price = (decimal) Model.Price;
    var sku = (string) Model.Sku;
}
<article>
    Arthor: @arthor<br/>
    Language: @language<br />
    Price: @price<br/>
    Sku: @sku
</article>